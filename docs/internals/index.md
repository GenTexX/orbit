# Internals

Architecture documentation for Orbit developers. The vocabulary lives in `CONTEXT.md` at the repo root; decisions live in the ADR section. Deep-dives on subsystems (renderer, Aurora, Comet pipeline) land here as they are built.
