
@export
let speed: f32;

@export
let amp: f32;

let i: f32;

func update(dt: f32) {

    transform.position.x = sin(i * speed) * amp * 2;
    transform.position.y = cos(i * speed) * amp * 4;

    i += 0.001;

}
