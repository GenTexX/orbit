// This file is wrong on purpose. Open it in the Code pane to see what the
// compiler says while you type - every mistake below has a squiggle waiting,
// and they all appear at once rather than stopping at the first.

let ready = true;

func update(dt: f32) {
    // A bool is not a number.
    transform.position.x += ready;

    // Nothing here is called `speed`.
    transform.position.y = speed;

    // print takes a String.
    print(dt);
}
