func clamp(value: f32, lo: f32, hi: f32) -> f32 {
    if value < lo {
        return lo;
    }
    if value > hi {
        return hi;
    }
    value
}

let speed = 80.0;

func update(dt: f32) {
    transform.position.x = clamp(transform.position.x + speed * dt, -300.0, 300.0);
}
